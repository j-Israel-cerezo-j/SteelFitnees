function loadingBlock(classLoading) {
    var loader = document.querySelector(classLoading);
    loader.style.display = 'block';
}
function loadingNone(classLoading) {
    var loader = document.querySelector(classLoading);
    loader.style.display = 'none';
}